import React from "react";
import styled from "styled-components";

const Container = styled.h2`
  background-color: skyblue;
  margin: 0;
  padding: 6px 18px;
`;

const NavBar = () => {
  return <Container>Task App</Container>;
};

export default NavBar;
